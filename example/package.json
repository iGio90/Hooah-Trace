{
  "name": "hooah-trace-example",
  "version": "1.0.0",
  "description": "Hooah playground",
  "private": true,
  "main": "agent.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile agent -o compiled_agent.js",
    "watch": "frida-compile agent -o compiled_agent.js -w"
  },
  "dependencies": {
    "@babel/core": "^7.1.2",
    "@babel/plugin-transform-runtime": "^7.1.0",
    "@babel/preset-env": "^7.1.0",
    "@babel/runtime-corejs2": "^7.1.2",
    "frida-compile": "^8.0.0",
    "hooah-trace": "^1.0.0"
  }
}
